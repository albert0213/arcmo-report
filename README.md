# Arcmo 리포트 · 2026.09.10

분석PC(`server.py`)에서 만든 리포트를 **서버 없이 열리는 정적 사본**으로 굳힌 것입니다.
화면은 앱의 리포트 화면과 같습니다. 읽기 전용이라 촬영·분석·선수 편집은 없습니다.

- 만든 때: 2026-09-11 00:35
- 리포트 38개 · 선수 5001, 5002, 5003, 5004, 5005, 5006, 5008, 5009, 5010

## 보기

GitHub Pages 주소로 그대로 열립니다. 특정 스윙 링크는 `?p=<선수ID>&r=<리포트ID>` 입니다.

로컬에서 확인하려면 정적 서버가 필요합니다 (`file://` 로 열면 브라우저가 fetch 를 막습니다):

```bash
python -m http.server 8080
```

## 다시 만들기

원본 저장소에서:

```bash
python tools/export_public_report.py --date YYYYMMDD --out <이 폴더>
```
